<h1>蔵書管理アプリケーション Shelf</h1>
<table class="table table-striped">
  <% @books.each do |book| %>
  <tr>
    <td><%= link_to book.title,book %></td>
    <td><%= book.authors %></td>
    <td><%= book.publish_year %></td>
  </tr>
  <% end %>
</table>
<%= link_to "新規登録", [:new, :book], :class => "btn btn-primary" %>
